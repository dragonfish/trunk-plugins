version: 0.1
actions:
  definitions:
    - id: bazel-build
      display_name: Bazel Build
      description: Run 'bazel build //...' before git push
      run: bash ${plugin}/actions/bazel-build/bazel-build.sh
      triggers:
        - git_hooks: [pre-push]
