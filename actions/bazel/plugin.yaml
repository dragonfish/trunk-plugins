version: 0.1
actions:
  definitions:
    - id: bazel-build-pre-push
      display_name: Bazel Build
      description: Run 'bazel build //...' before git push
      run: bash ${plugin}/actions/bazel/bazel-build-pre-push.sh
      triggers:
        - git_hooks: [pre-push]
